# 会议纪要 — v1.3 Sprint 启动 & 并行分工

| 字段 | 内容 |
|------|------|
| **会议日期** | 2026-03-02 |
| **类型** | Sprint 启动会（Kickoff）+ 任务分工 |
| **主持** | Brian |
| **参会角色** | Brian · PM · Dev · Code-Reviewer |
| **关联纪要** | [2026-03-01-v1.2-release-expanded-planning.md](2026-03-01-v1.2-release-expanded-planning.md) |
| **Sprint 目标** | v1.3 第1周：基础设施清账（I-01·P-03） + 独立 Track 同步推进 |

---

## 一、Brian 主持词

> 今天正式启动 v1.3 Sprint。昨晚规划会议定下了 5 项决议和 7 条 DoD，今天开始执行。
>
> 原则：**凡无依赖的工作立刻并行，有依赖的等前置 AC 通过后再合并**。代码冲突由 Brian 协调，merge 顺序按依赖链执行。
>
> 今天的分工如下。

---

## 二、并行工作分工（第1周）

### 依赖关系图

```
I-01 (Alembic 统一)
 └── P-03 (abort_flags TTL)
      └── P-01 (Legacy 分支清除)
           ├── P-04 (并发锁)
           ├── P-09 (SubscriptionCrawlManager 接管)
           └── F-01 (飞书双路径合并) [第3周]

S-01/step1 (User 表)           ← 独立，第2周开始
 └── S-01/step2 (JWT 登录接口)
      └── S-01/step3 (JWT 中间件)
           └── S-02 (X-API-Key)
                └── W-01 (前端路由守卫)

C-01 (B站/微博日期过滤)         ← 独立，与 I-01 并行
C-05 (进度解析增强)              ← 独立，与 I-01 并行
D-02 (Pipeline 文档)            ← 独立，与 I-01 并行
D-03 (部署运维手册)              ← 独立，可随时推进
```

### Track A — 基础设施（主链路）

**负责人**：Dev
**状态**：🔄 进行中

| # | 任务 | AC | 状态 |
|---|------|----|:----:|
| A-1 | I-01: Alembic 统一 | `create_tables()` 只建 legacy 表；lifespan 调用 `alembic upgrade head`；新/升级部署均测试通过 | 🔄 |
| A-2 | P-03: abort_flags TTL | 改用 `TTLCache(maxsize=1000, ttl=3600)`；执行完成时主动删除；多 worker 场景写 `TaskExecution.is_aborted` 列 | 🔄 |
| A-3 | P-01: Legacy 分支清除 | `_run_task` legacy 代码块删除；存量任务迁移脚本；CI 通过 | ⏳ 等 P-03 |
| A-4 | P-04: 并发锁 | APScheduler 触发口加锁；并发测试通过 | ⏳ 等 P-01 |
| A-5 | P-09: SubscriptionCrawlManager 接管 | SubscriptionCrawlStep 改调 `enqueue()`；scheduler_service 遍历代码删除 | ⏳ 等 P-01 |

**合并顺序**：A-1 → A-2 → A-3 → A-4 + A-5（可同时 PR）

---

### Track B — 安全层（第2周，独立）

**负责人**：Dev（第2周开始）| **前置**：I-01 AC 通过

| # | 任务 | 预估 |
|---|------|:----:|
| B-1 | User 表 + bcrypt + Alembic `0002` | 0.5d |
| B-2 | JWT 登录接口 | 1d |
| B-3 | FastAPI JWT 中间件 | 1d |
| B-4 | X-API-Key 外部触发 | 1d |
| B-5 | 前端路由守卫 | 0.5d |

---

### Track C — 爬虫增强（本周，与 A 并行）

**负责人**：Dev | **无 Track A 依赖**

| # | 任务 | AC |
|---|------|----|
| C-1 | C-05: 进度解析增强 | B站/知乎/快手中文分页模式 `"第N页"` + 实际采集数计数修正 |
| C-2 | C-01 part: B站/微博日期过滤 | `BILI_DATE_START/END`、`WEIBO_DATE_START/END` env var 注入 + 爬虫适配 |

**合并**：C-1 和 C-2 互相独立，可同时 PR，Brian 按序合并。

---

### Track D — 文档（本周，与 A/C 并行）

**负责人**：Dev | **无任何代码依赖**

| # | 任务 | AC |
|---|------|----|
| D-1 | D-02: Pipeline 步骤参考文档 | `docs/reference/pipeline-steps.md` 覆盖所有 step_type 的参数表格 |
| D-2 | D-03: 部署运维手册 | `docs/ops/deployment.md` 含 Docker/uv/systemd 三种模式 |

**合并**：纯文档 PR，冲突概率极低，可直接 push。

---

## 三、Brian 协调约定

1. **合并顺序**：所有 Track A 的提交必须按 A-1 → A-2 → A-3 → A-4+A-5 顺序合并到 `dev`
2. **并行 PR 可同时开放**但不能乱序合并：C-1/C-2/D-1/D-2 的 PR 在 A-1 合并后均可随时合并
3. **进度同步**：每个 AC 通过后更新本文件状态列（`🔄` → `✅` / `⏳` → `🔄`）
4. **冲突处理**：如 Track B 的 User 表 Alembic 迁移与 Track A 的 `0001` 冲突，Brian 统一 rebase
5. **第2周开始**：Track A 全部完成（A-1~A-5 AC 通过）后，Brian 给出 "Track B green light" 信号

---

## 四、今日立即执行事项

| 事项 | 执行者 | 预计完成 |
|------|--------|---------|
| ① I-01: `db_session.create_tables()` 只建 legacy 表 | Dev | 今天 |
| ② I-01: `api/main.py` lifespan 加 alembic upgrade 调用 | Dev | 今天 |
| ③ P-03: `abort_flags` TTL 改造 | Dev | 今天 |
| ④ D-02: `docs/reference/pipeline-steps.md` 创建 | Dev | 今天 |
| ⑤ C-05: `crawler_manager._update_progress()` 补全中文分页 | Dev | 今天 |

---

## 五、决议确认

以下为昨晚会议五项决议的执行状态确认：

| 决议 | 状态 | 备注 |
|------|:----:|------|
| 决议1：v1.3 前两周不接受新功能 PR | ✅ 确认 | Brian 把关 |
| 决议2：Alembic 唯一入口（I-01 生效后） | ✅ 确认 | Code-Reviewer 审查模板加入必检项 |
| 决议3：CrawlerPool 锁定 v1.3 后 | ✅ 确认 | — |
| 决议4：v1.3 release gate 7 项 DoD | ✅ 确认 | PM 持有 checklist |
| 决议5：P-04 并发锁本周交付 | ✅ 确认 | 随 P-01 同批 |

---

*记录人：Brian | 存档：docs/ops/meetings/*
