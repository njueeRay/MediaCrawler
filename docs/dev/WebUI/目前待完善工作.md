# WebUI 目前待完善工作（Phase 5）

> 日期：2026-02-12  
> 目标：解决“订阅管理未真正跑通 + 配置项/变量不对齐 + .env.example 不完整”三大问题。

---

## 0. 结论先行（当前阻塞）

### P0 阻塞项
1. **订阅管理链路未真正跑通（已修复核心阻塞，仍需补齐联调）**  
  - ✅ 已修复 `wechat` 平台无法触发采集的 schema 阻塞（`CrawlerStartRequest.platform` 缺少 `wechat`）
  - ✅ 已补齐 `SaveDataOptionEnum` 对 `postgres` 的支持
  - ⏳ 仍需补：订阅维度调度模板、批量触发与执行可视化
2. **配置变量存在“多源定义”风险（本轮已完成第一轮对齐）**  
  - ✅ 飞书 Token 命名对齐：WebUI 主键改为 `FEISHU_BITABLE_APP_TOKEN`，并保留 `FEISHU_APP_TOKEN` 兼容读取
  - ✅ `config/router/service` 已完成首轮联动修正
3. **.env.example 覆盖不完整（本轮已补齐关键缺口）**  
  - ✅ 已补微信关键字段：`WECHAT_AUTH_KEY`、`WECHAT_APP_SECRET`
  - ✅ 已补微信细粒度字段：重试、间隔、格式、图片并发、目录、日期范围、付费过滤等
  - ✅ 已补飞书表格相关字段：`FEISHU_TABLE_ID`

### P1 体验/可观测性问题（本轮已补关键能力）
1. **开发模式 WebSocket 日志不通（已修复）**
  - ✅ 原因：Vite dev proxy 未开启 WS 转发
  - ✅ 修复：`webui-src/vite.config.ts` 的 `/api` 代理增加 `ws: true`
  - ✅ 回归：新增 `test/test_ws_logs_smoke.py`（连接 `/api/ws/logs`，ping→pong）
2. **DB 模式采集后“网页看不到数据”（已补齐 DB 浏览能力）**
  - ✅ 原因：DataExplorer 仅支持浏览 `data/` 文件，缺少 DB 记录查询接口
  - ✅ 修复：新增 `/api/data/db/tables`、`/api/data/db/records`、`/api/data/db/stats`，前端 DataExplorer 按存储模式自适配
  - ✅ 回归：新增 `test/test_db_browse_e2e.py`（CSV→sqlite 往返 + tables/records）

---

## 1. 下一阶段工作总览

## 1.1 Phase 5-A：订阅管理“真运行”闭环（P0）

### 目标
实现完整闭环：**搜索创作者 → 添加订阅 → 定时/手动采集 → 数据入库/落盘 → 页面可检索与筛选**。

### 后端任务
- [ ] `api/services/subscription_service.py`
  - [ ] `search_creators()` 接入真实平台搜索（至少先打通微信 + 小红书/微博其中一个）
  - [ ] 统一返回结构：`platform, creator_id, creator_name, avatar, desc, followers, extra`
- [ ] `api/routers/subscription.py`
  - [x] 新增“批量采集入队”接口：`POST /subscribe/crawl/batch`
  - [x] 新增“采集队列状态查询”接口：`GET /subscribe/crawl/status`
  - [x] 新增“最近一次采集状态”持久化（落库 + 重启恢复）
- [ ] `api/services/scheduler_service.py`
  - [ ] 订阅维度任务模板（按平台、按作者、按时间窗）
  - [ ] 失败重试与失败原因落库
- [ ] 数据落地
  - [ ] DB 模式：写入标准化表
  - [ ] CSV 模式：按平台目录落盘，并可回填浏览 API

### 前端任务
- [x] 订阅管理页增加：采集状态列（queued/running/success/failed）
- [x] 批量采集入口（表格勾选 + 批量采集按钮）
- [ ] 批量启停订阅
- [ ] 订阅列表支持“按平台/状态/最近更新时间”筛选

### 验收标准
- [ ] 新增订阅后 1 分钟内可见可追踪采集记录
- [ ] 至少 1 个平台可完成端到端（搜索→订阅→采集→展示）

### 本轮已执行（2026-02-12，5.4c）

- [x] 新增状态表：`webui_subscription_crawl_status`
- [x] `subscription_crawl_manager` 状态变更落库（queued/running/success/failed）
- [x] 服务重启后状态恢复：
  - queued -> 自动恢复入队
  - running -> 标记 failed（服务重启中断）
- [x] 新增验证脚本：`test/test_subscription_crawl_status_persistence.py`

---

## 1.2 Phase 5-B：配置系统单一真源（P0）

### 目标
确保 **config 目录是唯一真源**，API 层只做“读取元数据 + 写入 .env + 热重载”。

### 设计原则
- `config/base_config.py`：变量定义与默认值（`_env(...)`）
- `api/services/config_service.py`：只基于“配置元数据注册表”构建 UI schema
- `api/routers/config.py`：只做入参校验 + service 调用

### 必做改造
- [x] 建立统一配置元数据表（建议：`config/config_meta.py`）
  - 字段：`key, group, label, type, default, required, options, secret, desc`
- [x] `config_service` 改为从 `config_meta` 生成分组与字段，禁止手写散落 key
- [x] 启动时执行一致性校验：
  - [x] `config_meta` 的 key 必须都存在于 `base_config.py`
  - [x] 前端提交 key 若不存在，直接拒绝并提示
- [x] 增加 `/api/config/validate`：
  - [x] 返回缺失必填项
  - [x] 返回类型错误项
  - [x] 返回当前模式依赖项（例如 sqlite/mysql/postgres）

  ### 本轮已完成（2026-02-12，第三批）
  - [x] `api/routers/config.py` 新增 `GET /config/validate`（缺失必填项 + 常见依赖项提示）

### 验收标准
- [x] 新增任一配置变量只需改 2 处：`base_config.py + config_meta.py`
- [x] 前端配置页自动出现新项，无需再改 router/service 代码

### 本轮已完成（2026-02-12）
- [x] `api/schemas/crawler.py`：`PlatformEnum` 增加 `WECHAT`
- [x] `api/schemas/crawler.py`：`SaveDataOptionEnum` 增加 `POSTGRES`
- [x] `api/services/config_service.py`：飞书 Token 主键改为 `FEISHU_BITABLE_APP_TOKEN`
- [x] `api/services/config_service.py`：增加 `FEISHU_TABLE_ID`
- [x] `api/services/config_service.py`：微信分组补齐细粒度配置项（auth/secret/retry/date/filter/download）
- [x] `api/routers/config.py`：移除未使用 `get_db` 导入，增强飞书 token 兼容检测

### 本轮继续完成（2026-02-12，第二批）
- [x] 新增 `config/config_meta.py`：配置元数据单一真源（分组/字段/敏感项）
- [x] `api/services/config_service.py`：改为从 `config_meta` 读取 `CONFIG_GROUPS`
- [x] `api/services/config_service.py`：`_find_group()` 改为 `config_meta` 路由，移除重复定义
- [x] 新增 `test/test_config_consistency.py`：校验 `config_meta`、`config/*.py` 与 `.env.example` 一致性
- [x] 新增 `test/test_subscription_wechat_e2e.py`：校验微信订阅触发采集不再 422

---

## 1.3 Phase 5-C：补齐 `.env.example`（P0）

### 目标
与前端配置页、后端真实读取项完全一致，可直接复制为 `.env` 使用。

### 验收标准
- [x] `.env.example` 覆盖率 = `config_meta.py` 100%
- [x] 复制 `.env.example -> .env` 后，WebUI 配置页不出现"未知字段/缺失字段"

### 当前状态
- ✅ `config_service` 已不再手写字段列表，改由 `config_meta` 驱动
- ✅ 已具备自动化一致性检查脚本，可用于 CI 前置校验

### 本轮已完成（2026-02-12）
- [x] 飞书 token 示例主键改为 `FEISHU_BITABLE_APP_TOKEN`，保留 `FEISHU_APP_TOKEN` 注释兼容
- [x] 新增 `FEISHU_TABLE_ID`
- [x] 微信配置从注释占位改为可直接启用的默认项（含 `WECHAT_APP_SECRET`）
- [x] 补齐微信细粒度配置默认值（重试、日期、格式、目录、并发、付费过滤）

---

## 1.4 Phase 5-D：全面回归 + 交互优化（P1）

### 目标
把“可用”变成“可持续可用”：将关键问题（WS 日志、DB 数据可视化、订阅批量采集）固化为可重复执行的 smoke/E2E，并在每次改动后可快速定位回归。

### 回归脚本（推荐顺序）
- [x] `test/test_webui_smoke_suite.py`：一键检查关键 HTTP/WS 端点（按 save_data_option 自动断言）
- [x] `test/test_api_response_contract_scan.py`：扫描 router handler 是否存在裸结构返回（契约门禁）
- [x] `test/test_ws_logs_smoke.py`：验证 `/api/ws/logs` 连接与 ping/pong
- [x] `test/test_db_browse_e2e.py`：验证 DB 浏览端点（CSV→sqlite）
- [x] `test/test_field_mapping_preview_e2e.py`：验证字段映射预览接口返回结构
- [x] `test/test_subscription_batch_crawl_e2e.py`：验证批量入队 + 状态查询
- [x] `test/test_subscription_e2e.py`：验证订阅闭环（搜索→订阅→采集→展示）
- [x] `test/test_config_consistency.py`：校验 `config_meta`/config/.env.example 一致性

### 验收标准
- [ ] dev 模式下 Logs 页面 30s 内必定连上 WS（失败要有可见错误信息）
- [ ] sqlite 模式下 DataExplorer 必定能看到“表/记录数/预览记录”（即使 0 记录也要正常展示）
- [ ] 批量采集入队后 5s 内 Subscription 页面能显示 queued/running 状态

---

## 1.5 Phase 5-E：接口响应契约治理（P0）

### 目标
消除“后端返回结构与前端解析不一致”导致的静默故障，建立可回归、可门禁的统一接口契约。

### 后端任务
- [x] `api/routers/crawler.py`：`/start`、`/stop`、`/status`、`/logs` 统一为 `{code,message,data}`
- [x] `api/routers/data.py`：`/files`、`/stats`、`/files/{path}` 预览、`/db/*` 统一为 `{code,message,data}`
- [x] 继续清理 `api/main.py` 与遗留接口中的裸结构返回（纳入契约扫描门禁）

### 前端任务
- [x] `webui-src/src/api/index.ts`：新增统一解包 `unwrapApiData()`
- [x] `Dashboard.vue`、`Logs.vue`、`DataExplorer.vue` 改为统一解包，减少散落兼容分支
- [x] 其余页面逐步收敛到统一解包（Config/Subscription/TaskScheduler/Feishu/FieldMapping）

### 验收标准
- [x] `api/routers/*.py` 中不再出现 router handler 直接 `return { ... }`
- [x] smoke 至少覆盖 `/crawler/logs`、`/data/files`、`/data/stats` 的响应形状断言
- [x] 新增契约扫描脚本并纳入 CI 前置门禁

### 本轮已执行（2026-02-12）
- [x] 新增并运行 `test/test_api_response_contract_scan.py`（本地通过）
- [x] smoke 已增加 `/crawler/logs` 响应形状断言
- [x] 新增 `.github/workflows/webui-contract.yml`（契约扫描 + WebUI build gate）

---

## 2. 变量对齐专项清单（必须执行）

- [x] 扫描 `config/base_config.py` 导出变量全集
- [x] 扫描 `api/services/config_service.py` 的 `CONFIG_GROUPS`（或等价结构）
- [x] 扫描 `api/routers/config.py` 入参/输出映射
- [x] 生成差异报告（多出/缺失/重名/类型不一致）
- [x] 按“base_config 真源”修正 API 层
- [x] 同步更新 `.env.example`
- [x] 写入 `docs/dev/WebUI/DEVLOG.md` 变更记录

### 本轮已执行（2026-02-12）

- [x] 新增并运行 `test/test_config_alignment_audit.py`，产出 `docs/dev/WebUI/变量对齐差异报告.md`
- [x] `api/routers/config.py`：`PUT /config` 增加入参白名单校验（`config_meta` + `FEISHU_APP_TOKEN` 兼容）
- [x] 审计结果：`config_meta` 与 `.env.example` 覆盖一致（缺失 0），`router` 白名单异常键 0，重复键 0，类型不一致 0
- [x] `.env.example` 与 `config_meta` 无新增差异，本轮无需补项

---

## 3. 测试计划（进入 Phase 4/5 联合验证）

## 3.1 API 自动化测试（新增）
- [x] `test/test_config_consistency.py`
  - [x] 校验 `base_config` 与 `config_meta` 一致
  - [x] 校验 `.env.example` 覆盖完整
- [x] `test/test_subscription_e2e.py`
  - [x] 搜索 → 订阅 → 触发采集 → 查询结果
- [ ] `test/test_storage_switch_e2e.py`（验收后迭代）
  - [ ] csv → sqlite → csv 往返切换
  - [ ] 切换后关键页面 API 均 200

### 本轮已补（2026-02-12）
- [x] `test/test_ws_logs_smoke.py`：验证 `/api/ws/logs` 可连接，ping→pong
- [x] `test/test_db_browse_e2e.py`：验证 DB 浏览端点（CSV 模式 400，sqlite 模式 tables/records 200）

## 3.2 前端联调验证
- [ ] 配置页保存后，立即生效（无需重启）
- [ ] 订阅页能看到真实搜索结果并成功入订阅列表
- [ ] 数据页能按日期范围筛选、预览与下载
- [ ] 飞书页可完成“连接检测 + 映射 + 同步历史”

---

## 4. 交付顺序建议（高速推进）

1. **先做 P0-1：配置真源与变量对齐**
2. **再做 P0-2：.env.example 补齐并通过一致性测试**
3. **最后做 P0-3：订阅真运行闭环 + E2E**

---

## 4.1 验收阶段入口（新增）

- 已形成验收收口方案：`docs/dev/WebUI/验收阶段计划.md`
- 建议先完成文档中的 3 个“验收必做项”，再进入正式签收。

---

## 5. 当日可执行命令（Windows）

````powershell
# 1) 运行后端
uvicorn api.main:app --host 127.0.0.1 --port 8080 --reload

# 2) 前端开发
cd webui-src
npm run dev

# 3) 运行测试（示例）
cd ..
uv run python test/test_ws_logs_smoke.py
uv run python test/test_db_browse_e2e.py