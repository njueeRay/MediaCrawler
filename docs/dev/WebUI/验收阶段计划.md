# WebUI 验收阶段计划（Phase 5 收口）

> 日期：2026-02-12（更新：2026-02-13）  
> 目标：判断是否可进入验收，并给出最小收口清单。

---

## 1. 结论

✅ 3 个“验收必做项”已全部完成（Phase 6 收口），**建议进入正式验收并准备签收**。

---

## 2. 验收必做项（阻塞签收）

1. **✅ 补齐 smoke 的响应形状断言**
   - 已完成：`test/test_webui_smoke_suite.py` 增加 `/data/files`、`/data/stats` 的 `{code,data}` 形状断言。

2. **✅ 完成“订阅真闭环”最小 E2E（B 站平台）**
   - 已完成：`test/test_subscription_e2e.py`（搜索→订阅→采集→展示）。

3. **✅ 修正文档勾选状态与真实状态不一致问题**
   - 已完成：`目前待完善工作.md`、`TODO.md`、`DEVLOG.md` 三者已对齐。

---

## 3. 可在验收后迭代（不阻塞签收）

- 订阅维度调度模板（`scheduler_service`）
- 失败重试策略与失败原因聚合展示
- 前端订阅列表高级筛选（平台/状态/最近更新时间）
- Dashboard 趋势图与采集频率图
- 小红书/抖音/快手搜索增强（会话依赖）

---

## 4. 建议验收流程

1. 先执行自动化门禁：
   - `test/test_api_response_contract_scan.py`
   - `test/test_config_alignment_audit.py`
   - `test/test_webui_smoke_suite.py`
2. 执行订阅闭环 E2E（新增后）：
   - `test/test_subscription_e2e.py`
3. 执行人工走查（30~60 分钟）：
   - 配置保存热生效
   - 订阅页搜索与采集状态追踪
   - DataExplorer 在 CSV/DB 下都可读

---

## 5. 决策建议

✅ 3 个“验收必做项”已全部完成，**建议进入正式验收并签收**。
